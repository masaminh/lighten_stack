name: test
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 
      - uses: asdf-vm/actions/install@v4
      - name: Enable Corepack (pin npm to packageManager)
        run: corepack enable
      - name: DEBUG npm version via corepack
        run: |
          echo "corepack npm --version: $(corepack npm --version)"
      - name: Verify package-lock.json is in sync
        run: |
          corepack npm install --package-lock-only --ignore-scripts
          git diff --exit-code package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Test
        run: npm run test
      - name: build
        run: npm run build
      - name: lint
        run: npm run lint
